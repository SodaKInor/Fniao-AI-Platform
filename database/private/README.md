# Private database inputs

This directory is only a local intake point for an approved database baseline used in isolated restore checks. Git tracks this README and the local ignore rule only.

Never commit database dumps, real records, credentials, exported configuration, generated logs or machine-specific paths. Obtain an authorized baseline through the existing private handoff process, verify it outside the repository, and remove local copies when they are no longer required.
