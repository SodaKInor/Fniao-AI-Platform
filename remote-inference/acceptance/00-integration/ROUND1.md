# 第一轮验收、集成和冻结记录

本轮按 01 全部通过 → 00 验收集成 → 02 的 2.1—2.5 → 00 验收冻结顺序完成。仅总表 1.1—1.6、2.1—2.5 共 11 项可勾选；4.7、后续客户端/业务/前端实现和真实服务验收保持未完成。

| 记录 | 完整提交 |
|---|---|
| 原始源码历史基线 | 8c7fa382a344e82eb13828d53b9fd9e018a5a461 |
| 本轮开始前共同提交 | e1ccab10fe5314c67be811c47cdbc3663e8a4b53 |
| 01 来源、备份、入口和十环境隔离交付 | 0514ccbe8e876dbdbae25ae8712db85487ba412c |
| 00 验收 01，释放 02 的集成提交 | 823eaa02050eda26c283a2e1588225d3ee022e8c |
| 02 内部业务 v1、外部草案和共享类型交付 | 5a55ca5cc6ea8fde09898f44519d62c715af12db |

唯一共同起点是包含本记录、最终验证和总表更新的冻结提交。其**完整 SHA 在代码工作树外的 00-integration/HANDOFF.md 与 WGAI-parallel/WORKSPACES.json 同时登记**，避免在提交内容中写入尚未形成的自身哈希。03、04a、04b 只允许快进至该 SHA；登记前再次核实三个工作树干净且无分叉。包内后续提交不改变这一历史起点。

## 证据和验证

- foundation-review.json：实际 01 提交与 HANDOFF、归属、来源/JAR/许可、独立构建、入口清单和全部环境核实。另独立重新计算库外四份备份归档哈希，均匹配 01 回执。原有服务恢复健康，未在现用数据卷恢复。
- contract-review.json：实际 02 提交、协议、共有状态/DTO、流、成果引用、原子任务边界和关键类型职责审阅。
- integrated-validation/：在集成后的文件上重新执行契约/样例和 AST/类型校验；证据只写到 00 目录，不改动 02 的回执。
- final-validation.json：完整后端既有 Docker 构建、OpenSpec 严格校验、Graphify 更新、范围和原服务检查。前端没有变化，沿用 01 已通过的前端构建证据；不重复宣称业务页面已验收。
- consumer-preflight.json：冻结前 03/04a/04b 的实际干净状态、分支及可快进性。快进后的完整 SHA 和复核结果见外层 HANDOFF 与工作区登记。

本轮未修改公共构建配置或依赖。00 的后端验证使用 01 的哈希检查脚本准备两个忽略的私有 JAR 和无密钥代码生成器资源，再从自身 worktree 按现有 Dockerfile 构建。隔离基线镜像仍对应 e1ccab1；后续包有实现变化时必须构建自己的镜像，不能把基线镜像当作新代码验收。

所有合并采用快进。01/02 交付提交保留，原 WGAI/main 仍在本轮开始前的提交；无远程推送、部署、业务入口删除、现用卷恢复或整体归档。

## 下一轮入口

03 实现 provider/client 与执行守卫；04a 实现资产、任务与成果；04b 实现业务前端。三个包在冻结后才可开始，各自沿用已有目录和分支。当前只同步共同起点，未实施三包任务。

真实外部接口仍未提供，外部规范保持草案。模拟样例和基线启动不计作真实 GPU、登录/权限端到端或正式服务验收。查询、取消、远端去重未确认均关闭；UNKNOWN 不自动重复推理。后续公开类型或协议变更必须交 02/00，不能各包改签名。
