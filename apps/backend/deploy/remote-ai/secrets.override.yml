# Optional overlay when service credentials are provisioned. Never put token contents in environment values.
services:
  backend:
    volumes:
      - type: bind
        source: ${WGAI_INFERENCE_TOKEN_SOURCE:?absolute backend-only credential file required}
        target: /run/secrets/wgai-inference-token
        read_only: true
