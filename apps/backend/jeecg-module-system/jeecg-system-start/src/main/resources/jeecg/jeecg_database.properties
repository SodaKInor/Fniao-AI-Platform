# The running application overwrites these compatibility defaults from
# spring.datasource.dynamic.datasource.master. They exist because the bundled
# code-generator library requires the resource during static initialization.
diver_name=com.mysql.cj.jdbc.Driver
url=jdbc:mysql://127.0.0.1:1/disabled
username=
password=
database_name=disabled
