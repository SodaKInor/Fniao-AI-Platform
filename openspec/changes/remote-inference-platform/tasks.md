## 1. 第 1 批：版本基线与改造分支

前置：当前工作区。交付：可回溯基线、备份说明、改造分支和入口清单。本批不改变业务行为，不依赖同事接口。全部批次均遵守 [design.md](design.md) 的“每批完成标准”。

- [x] 1.1 复核当前源码、运行配置、原 ZIP / 私有依赖来源，记录管理入口与核心服务状态；交付可追溯的基线清单，不把首页正常当成登录和推理已验收。
- [x] 1.2 准备 Git 忽略规则和无密钥配置/私有制品获取说明；复核候选提交文件不包含真实凭据、原始转储、权重、上传素材、缓存或 backend-master，许可证与来源说明保留。
- [x] 1.3 在现有根目录建立 Git 仓库，保存 main 基线后创建 feature/remote-inference 改造分支；记录基线提交标识，确认工作分支和提交范围正确，不创建重复工程或远程仓库。
- [x] 1.4 制作当前数据库与资产的一致备份并保存在版本库之外，记录恢复步骤及备份对应版本；验证文件可读取、备份清单与校验值齐全，不在现用数据卷上做恢复演练。
- [x] 1.5 根据 audit.md 核对实际路由、菜单和调用点，交付“保留管理 / 迁移执行 / 停用 / 需求待定”清单；逐项记录证据和处置，需求待定项不列入源码或数据删除清单。
- [x] 1.6 按并行工作包清单建立独立 Git worktree / 分支、工作目录登记和运行资源分配；验证各目录的 Git 根路径、分支和写入范围独立，既有端口/数据库/数据卷不被共用，未创建的工作区保持待准备状态。

## 2. 第 2 批：接口契约与模拟样例

前置：第 1 批。交付：本项目可据以开发的契约草案与样例。已有同事接口资料时直接适配；未提供时保留“待双方确认”，真实确认在第 5 批完成。

- [x] 2.1 在 backend-github/integrations/ai-contracts 编写版本化请求/成果契约，列出方法、鉴权、身份字段、输入、输出、错误和限制；验证契约可解析，区分已提供值与待填项，不把示例 URL 当真实地址。
- [x] 2.2 准备一个文件输入、结构化结果及成果文件的最小成功样例，另附有效空成果和错误样例；验证字段、媒体类型与契约一致，所有模拟响应明确标识为测试数据，不冒充实际算法能力。
- [x] 2.3 约定前端业务 API 与外部适配边界、200/202 本地任务语义及 UNKNOWN 含义；核对普通同步接口也能接入，外部查询/取消/去重默认未启用。
- [x] 2.4 形成交接记录模板：开发与正式地址、凭据交付方式、最大输入/时长/并发、成果有效期和可选能力；验证各缺项注明最迟所需批次，开发和正式确认记录分别保留，不要求同事提交 GPU 源码。
- [x] 2.5 按架构约束交付最小共享领域类型、端口接口及请求/成果 DTO，冻结前后端业务契约和 Java 调用边界；验证公共类型不依赖 HTTP/数据库实现，样例符合契约，并交付新增业务文件规模及分层依赖检查，使客户端、文件与任务后端、前端可独立开发。
- [x] 2.6 02 从第四轮唯一放行提交的祖先快进，将业务契约提升为 `1.1.0`；冻结兼容的图片接口、`POST /ai/v1/video-jobs`、`POST /ai/v1/jobs/{id}/cancel`、有界视频/未知原因 DTO、OpenAPI 与正反样例，禁止无约束 Map。
- [x] 2.7 02 与伴随 `remote-video-streaming` 共同冻结流来源、会话、事件、截图和停止契约；00 核实真实接口假设、文件归属和样例后记录新共同起点，未通过前不释放 03。

## 3. 第 3 批：后端调用基础与执行边界

前置：第 2 批草案和样例可用。交付：可通过本地协议夹具验证的调用客户端及基础守卫。本批不需要 GPU 在线。

- [x] 3.1 建立统一客户端边界和 disabled/mock/remote 模式；按第 1 批入口清单检查所有保留的执行入口，验证 remote/disabled 不触发旧 Java 算法，服务离线时不自动回退到旧模型。
- [x] 3.2 实现独立服务凭据、批准地址/可信 CA、连接/处理/传输超时和并发限额配置，提供 backend-github 下的开发/正式模板及部署增量；验证配置有实际消费者，秘密不进入浏览器或普通日志，不新增 GPU 部署工程。
- [x] 3.3 实现请求/错误转换和针对性的协议测试夹具；验证正常响应、有效空成果、连接失败、外部鉴权失败及协议不兼容，GPU 凭据错误不会清除业务用户登录。
- [x] 3.4 收紧旧 AI 匿名执行入口及操作权限，按清单停用缺后端页面的运行入口；验证绕过菜单直接请求仍被拒绝，保留的管理查询正常，新任务不复用旧匿名 WebSocket 传输结果。
- [x] 3.5 实现初版能力可用性接口并区分核心健康与外部可达性；用配置缺失、能力停用、模拟外部离线验证状态反馈，外部无模型就绪信息时不宣称模型已就绪。
- [x] 3.6 03 从新共同起点实现真实图片、上传视频和流会话 provider 方法，严格转换 `1.1.0` DTO；验证正常、有效空成果、协议错误和供应商扩展字段，响应未知不得透明重发推理 POST。
- [x] 3.7 03 实现批准地址、TLS/CA、独立服务鉴权及图片/视频/流分项能力门禁；真实资料缺项时保持 disabled，GPU 凭据错误不得清除业务用户登录，并提交独立 HANDOFF、证据和 Graphify 更新供 00 验收。

## 4. 第 4 批：前后端模拟完整流程

前置：第 2 批公共契约冻结后，文件与任务后端（04a）和前端（04b）可与第 3 批并行实现；第 3 批与两个工作包合并后由集成包（00）验收 4.7。交付：第一个可演示版本，可上传、提交、等待、保存、查看和下载模拟成果。权限与基本故障保护在本批生效。

- [x] 4.1 增量建立资产、调用记录、能力绑定和必要状态事件结构，保存归属及非敏感配置快照；在已有数据库副本验证迁移和重复执行检查，不清空历史数据或重跑初始化清理 SQL。
- [x] 4.2 实现私有资产上传、归属校验和授权下载，以有界内存发送文件/JSON 输入；验证超限输入在外呼前拒绝、跨用户访问被拒绝、旧匿名静态路径不能读取新 AI 私有文件。
- [x] 4.3 实现持久调用 ID、有界后台派发与短等待 200/202；验证受理前已保存记录、请求超过前端等待预算后后台仍等待同一调用，已发送但响应丢失时记录 UNKNOWN 且不自动重发。
- [x] 4.4 实现同用户幂等 key 与规范化请求摘要的本地去重；以重复/并发提交验证同一请求只自动派发一次，同 key 不同输入返回冲突，用户刷新不创建新调用。
- [x] 4.5 实现结构化成果保存和本批契约中使用的文件流/下载引用适配；验证下载地址与重定向受限、长度/校验值可用时核对，文件完整回存后才标记成功，模拟服务离线后历史仍可读。
- [x] 4.6 实现统一前端 API、能力入口、状态查询、错误说明及成果预览下载；用样例验证本批约定类型可展示，页面不暴露 GPU 地址、密钥、权重路径或显卡配置，未支持类型不显示为可用。
- [x] 4.7 通过页面演示“上传 -> 提交 -> 等待 -> 查看/下载 -> 刷新后查看历史”，补充下载中断和权限拒绝验收；记录演示样例、结果与批次版本，明确这是模拟闭环而非真实 GPU 联调。
- [x] 4.8 04a 泛化持久任务与成果，补齐 PENDING 恢复、`FETCHING_RESULT` 只重取成果、取消基础、有界视频事件/截图/可选标注视频存储；与流变更应用 V002 增量迁移并两次执行核对旧图片历史、表和行摘要。
- [x] 4.9 04b 增加上传视频任务页、事件时间线和授权截图预览，并接入伴随变更的流页面；轮询具备离开清理、代次隔离、事件去重和终态保护，不显示 GPU/RTSP/凭据。
- [x] 4.10 00 按 03→04a→04b 串行核实 HANDOFF、文件归属、真实接口语义与构建/回归证据，每包验收后才释放下一包；任何越权、重复派发、UNKNOWN 误标或历史不兼容均退回原包。

## 5. 第 5 批：独立 HTTP 占位服务与本地联调

前置：第 4 批完成。交付：业务后端通过真实 HTTP provider 适配器调用独立 stub，完成图片、上传视频和实时流的可重复组合验收。本批不需要同事服务，不产生真实 GPU 验收结论。

- [x] 5.1 在现有 provider 草案中冻结 stub 可实现的图片、视频、流、成果下载和故障场景；补齐模拟来源字段及正反 fixtures，验证契约可解析且不改变业务 API `1.1.0` 的既有兼容行为。
- [x] 5.2 在独立目录实现 HTTP stub，并拆分启动、配置、鉴权、路由、请求校验、场景服务、响应编码和测试；验证它不读取业务数据库、不调用旧算法、不包含 GPU/模型依赖，且没有单文件承载完整服务。
- [x] 5.3 为 stub 增加显式开发 Compose profile 和无密钥配置模板；验证业务后端在 remote 模式经容器网络调用 stub，正式配置不启动、不引用也不自动回退 stub。
- [x] 5.4 用 remote→stub 完成图片上传与成果、视频事件与截图、流来源/会话/事件/停止的组合验收；证据串联 requestId/sessionId、资产哈希和契约版本，并在结果、日志、页面和报告中明确标识模拟。

## 6. 第 6 批：本地故障、恢复与观测验收

前置：第 5 批 stub HTTP 闭环。交付：不依赖真实 GPU 的确定性、恢复、取消/停止和观测证据；真实 provider 特性仍不得由 stub 推断。

- [x] 6.1 完善重启恢复和安全重试：PENDING 可恢复，已派发且不可查询保持 UNKNOWN，新显式尝试关联原记录，`FETCHING_RESULT` 只重取成果；用 stub 在不同派发时刻验证无盲目重复提交。
- [x] 6.2 完成 PENDING 取消以及 provider 明确支持时的远程取消与流停止；用 stub 验证竞争、权限、不支持、响应丢失、迟到结果和终态保护，只有确认后才进入取消或停止终态。
- [x] 6.3 补充 requestId/sessionId 阶段耗时、错误分类、队列/在途数量、事件去重计数、Micrometer 指标与滚动日志；验证 stub 下线不影响核心管理，响应和日志无凭据、原始地址或 RTSP 秘密。
- [x] 6.4 用 stub 覆盖成果断流、响应丢失、格式错误、有效空结果、事件重复/乱序、迟到事件、重复点击和后端重启；逐项记录前端状态、外部调用次数和文件结果。
- [x] 6.5 00 合入 05 与 06 后统一回归登录/越权、图片/视频/流、重复提交、UNKNOWN、取消/停止边界、provider 离线历史访问和用户会话；只生成“本地 stub/disabled 候选”验收 SHA，不标记真实 5070/4090 已通过。

## 7. 第 7 批：功能模块迁移与旧业务清理

前置：6.5 本地验收 SHA、最终保留/退役清单和模块依赖矩阵。交付：按功能分包的业务代码及可回退的旧功能清理。

- [x] 7.1 建立 capability、asset、job、result、image、video、stream、provider、operations、legacy 的后端/前端文件映射和允许依赖矩阵；用 Graphify 与 import 检查验证现状并确定逐组迁移顺序，不创建 audio/chat/training 空模块。
- [x] 7.2 将后端 AI 代码按功能分包且在功能内部保留必要 api/application/domain/port/persistence 层；逐模块编译和测试，验证 Controller、provider、存储与持久化职责没有越界或循环依赖。
- [x] 7.3 将前端 AI 页面、API、服务、组件和路由归入对应功能模块；验证图片、上传视频、实时流、任务历史和结果下载行为不变，轮询代次与页面销毁清理继续通过。
- [x] 7.4 退役全部 MaxKB、tchat、easyAi 聊天和训练执行入口；按引用清单分组清理旧 `AIModel`、本地图片/视频/语音/OCR/训练执行代码，保留历史、管理 CRUD 和仍被使用的通用组件。根据用户后续将在新目录重构的决定，无活动调用者的 native 依赖和配套构建脚本也保留为预留能力，但不得接回已退役入口。
- [x] 7.5 每组前后验证引用、前后端构建、remote→stub 流程、disabled 模式、历史成果、数据库表/行摘要和 Graphify；独立提交并记录回滚点，真实服务未知项保持 disabled。

## 8. 第 8 批：最终仓库目录整合与本地交付

前置：第 7 批完成且业务行为回归通过。交付：单一、可独立克隆的 `Fniao-AI-Platform` 仓库和本地 stub/disabled 发布候选。

- [ ] 8.1 由 08-release 从 `a14450ec0ed82cd329a666e52ac12c15cce3515d` 或包含它的最新规划提交开始，用 `git mv` 仅将 `backend-github`、`frontend-vue` 迁到 `apps/backend`、`apps/frontend`；更新 Maven、npm、测试、许可证和最小构建入口，00 验收后冻结 `08A_SHA`。
- [ ] 8.2 由 08b-database-layout 从 `08A_SHA` 建立 `database/{bootstrap,migrations/ai-core,migrations/stream,seeds/stub,private}`；保留 V001/V002 字节、校验值和顺序，代码生成器 SQL 留在所属源码，私有数据保持忽略，并在数据库副本验证初始化和重复迁移。
- [ ] 8.3 由 08c-remote-boundary 与 8.2 并行，将契约、fixtures、stub、验收证据、handoff、非数据库远程部署文件和架构文档分别迁入 `remote-inference`、`deploy/remote-inference` 与 `docs/remote-inference`；逐文件移动避免接管 8.2 的迁移和 seed，验证不存在两份可执行规范来源。
- [ ] 8.4 00 合入 8.2/8.3 并冻结 `08BC_SHA` 后，由 08-release 更新 Compose、Dockerfile、环境模板、备份恢复脚本、AGENTS、OpenSpec 链接、Graphify 和 Serena 管理脚本，使活动路径从 Git 根动态解析；验证正式 Compose 默认无 stub，旧 WGAI 与 worktree 绝对路径不出现在活动配置。
- [ ] 8.5 由 08-release 执行 Java、Vue、数据库、Compose、权限、remote→stub、disabled、历史读取、秘密扫描、文件规模、模块依赖和两个 `openspec validate --strict`；生成最终集成报告与本地 RC，明确真实 GPU 门禁未完成。
- [ ] 8.6 由 00 验收 08 的结构提交后合入并推送 main，从远程 main 克隆 `/Users/twowt88/Documents/ChatGPT/Fniao-AI-Platform`；确认它不共享旧 `.git`，不包含并行目录、backend-master 或旧工具缓存，并在新目录重新建立 Graphify、Serena 和 OpenSpec 定位。

## 9. 第 9 批：真实 GPU 服务验收与正式交付

前置：同事提供服务、接口资料、网络和凭据。交付：先完成 RTX 5070 局域网验收，再完成 RTX 4090 48GB 正式验收；任一条件缺失时保持本批未完成。

- [ ] 9.1 对齐 RTX 5070 的方法、路径、TLS/CA、鉴权、图片/视频/流样例、错误确定性、输入限额、并发、成果有效期及查询/取消/停止/去重能力；逐能力记录证据，缺项能力保持 disabled。
- [ ] 9.2 从实际业务后端容器请求局域网固定地址，完成路由、鉴权、加密传输和代表性素材验收；不以 ping、端口可达、stub 或宿主机请求替代应用请求。
- [ ] 9.3 分别完成真实图片、上传视频和可用实时流的请求、成果回存、页面展示与历史读取；以 request/session ID、资产哈希、服务版本和契约版本串联证据。
- [ ] 9.4 按真实 provider 能力复验响应丢失、查询、取消/停止、断流、恢复、事件去重、限额和并发；不擅自停止同事服务，无法安全注入的场景明确记录范围。
- [ ] 9.5 对接 Ubuntu 单张扩容 48GB RTX 4090 正式服务，执行正式权限、限额、成果、数据库/文件恢复及 disabled/配置回滚验收；不以 5070 记录替代正式证据。
- [ ] 9.6 汇总真实接口、发布制品、镜像摘要、迁移集合、运行与排障文档；仅当所有当前必做任务有真实证据时同步主规格并归档，外部条件仍缺失时保持变更开放。
