## 1. 第 1 批：版本基线与改造分支

前置：当前工作区。交付：可回溯基线、备份说明、改造分支和入口清单。本批不改变业务行为，不依赖同事接口。全部批次均遵守 [design.md](design.md) 的“每批完成标准”。

- [x] 1.1 复核当前源码、运行配置、原 ZIP / 私有依赖来源，记录管理入口与核心服务状态；交付可追溯的基线清单，不把首页正常当成登录和推理已验收。
- [x] 1.2 准备 Git 忽略规则和无密钥配置/私有制品获取说明；复核候选提交文件不包含真实凭据、原始转储、权重、上传素材、缓存或 backend-master，许可证与来源说明保留。
- [x] 1.3 在现有根目录建立 Git 仓库，保存 main 基线后创建 feature/remote-inference 改造分支；记录基线提交标识，确认工作分支和提交范围正确，不创建重复工程或远程仓库。
- [x] 1.4 制作当前数据库与资产的一致备份并保存在版本库之外，记录恢复步骤及备份对应版本；验证文件可读取、备份清单与校验值齐全，不在现用数据卷上做恢复演练。
- [x] 1.5 根据 audit.md 核对实际路由、菜单和调用点，交付“保留管理 / 迁移执行 / 停用 / 需求待定”清单；逐项记录证据和处置，需求待定项不列入源码或数据删除清单。
- [x] 1.6 按并行工作包清单建立独立 Git worktree / 分支、工作目录登记和运行资源分配；验证各目录的 Git 根路径、分支和写入范围独立，既有端口/数据库/数据卷不被共用，未创建的工作区保持待准备状态。

## 2. 第 2 批：接口契约与模拟样例

前置：第 1 批。交付：本项目可据以开发的契约草案与样例。已有同事接口资料时直接适配；未提供时保留“待双方确认”，真实确认在第 5 批完成。

- [x] 2.1 在 backend-github/integrations/ai-contracts 编写版本化请求/成果契约，列出方法、鉴权、身份字段、输入、输出、错误和限制；验证契约可解析，区分已提供值与待填项，不把示例 URL 当真实地址。
- [x] 2.2 准备一个文件输入、结构化结果及成果文件的最小成功样例，另附有效空成果和错误样例；验证字段、媒体类型与契约一致，所有模拟响应明确标识为测试数据，不冒充实际算法能力。
- [x] 2.3 约定前端业务 API 与外部适配边界、200/202 本地任务语义及 UNKNOWN 含义；核对普通同步接口也能接入，外部查询/取消/去重默认未启用。
- [x] 2.4 形成交接记录模板：开发与正式地址、凭据交付方式、最大输入/时长/并发、成果有效期和可选能力；验证各缺项注明最迟所需批次，开发和正式确认记录分别保留，不要求同事提交 GPU 源码。
- [x] 2.5 按架构约束交付最小共享领域类型、端口接口及请求/成果 DTO，冻结前后端业务契约和 Java 调用边界；验证公共类型不依赖 HTTP/数据库实现，样例符合契约，并交付新增业务文件规模及分层依赖检查，使客户端、文件与任务后端、前端可独立开发。
- [x] 2.6 02 从第四轮唯一放行提交的祖先快进，将业务契约提升为 `1.1.0`；冻结兼容的图片接口、`POST /ai/v1/video-jobs`、`POST /ai/v1/jobs/{id}/cancel`、有界视频/未知原因 DTO、OpenAPI 与正反样例，禁止无约束 Map。
- [x] 2.7 02 与伴随 `remote-video-streaming` 共同冻结流来源、会话、事件、截图和停止契约；00 核实真实接口假设、文件归属和样例后记录新共同起点，未通过前不释放 03。

## 3. 第 3 批：后端调用基础与执行边界

前置：第 2 批草案和样例可用。交付：可通过本地协议夹具验证的调用客户端及基础守卫。本批不需要 GPU 在线。

- [x] 3.1 建立统一客户端边界和 disabled/mock/remote 模式；按第 1 批入口清单检查所有保留的执行入口，验证 remote/disabled 不触发旧 Java 算法，服务离线时不自动回退到旧模型。
- [x] 3.2 实现独立服务凭据、批准地址/可信 CA、连接/处理/传输超时和并发限额配置，提供 backend-github 下的开发/正式模板及部署增量；验证配置有实际消费者，秘密不进入浏览器或普通日志，不新增 GPU 部署工程。
- [x] 3.3 实现请求/错误转换和针对性的协议测试夹具；验证正常响应、有效空成果、连接失败、外部鉴权失败及协议不兼容，GPU 凭据错误不会清除业务用户登录。
- [x] 3.4 收紧旧 AI 匿名执行入口及操作权限，按清单停用缺后端页面的运行入口；验证绕过菜单直接请求仍被拒绝，保留的管理查询正常，新任务不复用旧匿名 WebSocket 传输结果。
- [x] 3.5 实现初版能力可用性接口并区分核心健康与外部可达性；用配置缺失、能力停用、模拟外部离线验证状态反馈，外部无模型就绪信息时不宣称模型已就绪。
- [ ] 3.6 03 从新共同起点实现真实图片、上传视频和流会话 provider 方法，严格转换 `1.1.0` DTO；验证正常、有效空成果、协议错误和供应商扩展字段，响应未知不得透明重发推理 POST。
- [ ] 3.7 03 实现批准地址、TLS/CA、独立服务鉴权及图片/视频/流分项能力门禁；真实资料缺项时保持 disabled，GPU 凭据错误不得清除业务用户登录，并提交独立 HANDOFF、证据和 Graphify 更新供 00 验收。

## 4. 第 4 批：前后端模拟完整流程

前置：第 2 批公共契约冻结后，文件与任务后端（04a）和前端（04b）可与第 3 批并行实现；第 3 批与两个工作包合并后由集成包（00）验收 4.7。交付：第一个可演示版本，可上传、提交、等待、保存、查看和下载模拟成果。权限与基本故障保护在本批生效。

- [x] 4.1 增量建立资产、调用记录、能力绑定和必要状态事件结构，保存归属及非敏感配置快照；在已有数据库副本验证迁移和重复执行检查，不清空历史数据或重跑初始化清理 SQL。
- [x] 4.2 实现私有资产上传、归属校验和授权下载，以有界内存发送文件/JSON 输入；验证超限输入在外呼前拒绝、跨用户访问被拒绝、旧匿名静态路径不能读取新 AI 私有文件。
- [x] 4.3 实现持久调用 ID、有界后台派发与短等待 200/202；验证受理前已保存记录、请求超过前端等待预算后后台仍等待同一调用，已发送但响应丢失时记录 UNKNOWN 且不自动重发。
- [x] 4.4 实现同用户幂等 key 与规范化请求摘要的本地去重；以重复/并发提交验证同一请求只自动派发一次，同 key 不同输入返回冲突，用户刷新不创建新调用。
- [x] 4.5 实现结构化成果保存和本批契约中使用的文件流/下载引用适配；验证下载地址与重定向受限、长度/校验值可用时核对，文件完整回存后才标记成功，模拟服务离线后历史仍可读。
- [x] 4.6 实现统一前端 API、能力入口、状态查询、错误说明及成果预览下载；用样例验证本批约定类型可展示，页面不暴露 GPU 地址、密钥、权重路径或显卡配置，未支持类型不显示为可用。
- [x] 4.7 通过页面演示“上传 -> 提交 -> 等待 -> 查看/下载 -> 刷新后查看历史”，补充下载中断和权限拒绝验收；记录演示样例、结果与批次版本，明确这是模拟闭环而非真实 GPU 联调。
- [ ] 4.8 04a 泛化持久任务与成果，补齐 PENDING 恢复、`FETCHING_RESULT` 只重取成果、取消基础、有界视频事件/截图/可选标注视频存储；与流变更应用 V002 增量迁移并两次执行核对旧图片历史、表和行摘要。
- [ ] 4.9 04b 增加上传视频任务页、事件时间线和授权截图预览，并接入伴随变更的流页面；轮询具备离开清理、代次隔离、事件去重和终态保护，不显示 GPU/RTSP/凭据。
- [ ] 4.10 00 按 03→04a→04b 串行核实 HANDOFF、文件归属、真实接口语义与构建/回归证据，每包验收后才释放下一包；任何越权、重复派发、UNKNOWN 误标或历史不兼容均退回原包。

## 5. 第 5 批：同一局域网真实联调

前置：第 4 批扩展及伴随流变更完成，同事提供 Ubuntu RTX 5070 上可调用的服务和完整资料。交付：图片、上传视频与实时事件的真实请求和成果验收。服务可达但资料不全只算预检，5.1保持未完成，相关能力保持 disabled。

- [ ] 5.1 05 对齐并记录 RTX 5070 的方法、路径、TLS/CA、鉴权、图片/视频/流样例、错误确定性、输入限额、并发、成果有效期、事件查询及查询/取消/停止/去重能力；缺任一适用资料时保持未完成，不虚构支持。
- [ ] 5.2 从实际运行的业务后端容器请求 GPU 固定地址，验证路由、监听/端口映射、服务鉴权和真实业务素材的加密通道；不以 ping、端口可达或宿主机请求代替应用请求验收。
- [ ] 5.3 将已确认能力配置为 remote，分别完成真实图片、上传视频和 RTSP 会话的请求、成果回存、页面展示与历史读取；以 request/session ID、资产哈希和接口版本串联证据，素材不得通过跨机本地路径。
- [ ] 5.4 按分项限额验证代表性素材、事件时间戳、截图和可选标注视频，记录耗时、大小、已知服务版本与回退方式；未确认的格式和能力保持 disabled，不把单样例成功扩展为全部媒体或正式环境验收。

## 6. 第 6 批：故障处理与恢复验收

前置：第 4 批数据/任务基础；真实服务相关验收依赖第 5 批。交付：稳定性与错误处理记录。本批可在等待同事接口期间先完成本地可验证部分。

- [ ] 6.1 06 完善重启恢复和安全重试：PENDING 可恢复，已派发且不可查询保持 UNKNOWN，新显式尝试关联原记录，`FETCHING_RESULT` 只重取成果；按不同派发时刻验证无盲目重复提交。
- [ ] 6.2 实现 PENDING 取消、已确认能力下的远程取消与流停止；验证取消/停止竞争、权限、停止不支持、响应丢失、迟到结果和终态保护，只有供应商确认后才进入取消/停止终态。
- [ ] 6.3 补充按 requestId/sessionId 的阶段耗时、错误分类、队列/在途数量、事件去重计数、Micrometer 指标与滚动日志；验证外部离线不影响核心管理，响应和日志无凭据或 RTSP 秘密。
- [ ] 6.4 用协议夹具覆盖成果断流、响应丢失、格式错误、事件去重、迟到事件、重复点击和后端重启；逐项记录前端状态、外部调用次数与文件结果，不擅自停止同事服务做故障注入。
- [ ] 6.5 00 严格按 05 后 06 合入，统一回归四账号登录/越权、图片/视频/流、重复提交、UNKNOWN、远程停止边界、GPU 离线历史访问和用户会话；5.1—5.4、6.1—6.5及流变更全部通过后才生成供07使用的验收SHA。

## 7. 第 7 批：旧业务迁移与无用依赖清理

前置：第 5、6 批完成，最终保留/退役清单明确。交付：首期发布候选版本。先迁移和停用，再按已验证清单删除无引用代码，不动同事 GPU 工程。

- [ ] 7.1 07 从 6.5 验收SHA快进，将保留的旧图片/上传视频动作逐项映射至统一任务和流会话；缺 provider `streamSourceId`、真实接口或成果约束的入口保持停用，禁止回退旧算法或传输明文 RTSP。
- [ ] 7.2 退役全部 MaxKB、tchat、easyAi 聊天及训练执行入口，前端菜单/页面与后端直接请求一致拒绝；保留数据库历史、已有成果、管理 CRUD、通用 WebSocket 和仍被引用的播放器资产。
- [ ] 7.3 按引用清单分组提交：先菜单/旧页面/聊天入口，再旧 `AIModel` 与本地图片/视频/语音/OCR/训练执行代码，最后仅清理确认无调用者的 OpenCV、ONNX Runtime、JavaCV、ASRT、RapidOCR、Tess4J 及相关构建脚本。
- [ ] 7.4 每组前后验证引用清单、前后端构建、真实远程流程、历史成果、数据库表/行摘要和 Graphify；交付清理对照表与 HANDOFF。00 合入后重跑完整门禁并记录 `remote-inference-rc1` 的唯一SHA、镜像摘要、契约版本、迁移集合和证据，不推送或部署。

## 8. 第 8 批：正式服务器接入与交付

前置：第 7 批发布候选通过，同事的正式服务和网络可用。交付：正式环境验收结果、运行与回滚说明。正式环境未提供时只准备资料，不勾选真实接入任务。

- [ ] 8.1 整理业务端发布制品、增量迁移及 Ubuntu 部署/配置说明，提供同一版本的开发与正式模板；验证切换目标只影响配置和已约定参数，不引入显卡专用业务分支，不编写 GPU 驱动或模型部署步骤。
- [ ] 8.2 对接同事部署的 Ubuntu 单张扩容 48GB RTX 4090 服务，填入正式地址、网络可达方式、凭据和限制；从正式业务后端验证连接、鉴权和成果取回，无服务或访问条件时保留本项未完成。
- [ ] 8.3 在正式配置下执行代表性业务请求及权限/限额验收，记录请求到成果的完整证据和双方接口版本；确认前端 API 保持兼容，按实际结果校准调用方超时/并发，不以 5070 联调记录替代正式验收。
- [ ] 8.4 在隔离环境恢复数据库与成果备份并验证记录/文件对应，演练 AI disabled 和兼容版本/配置回滚；验证核心管理与历史成果可用，不删除现用卷、不重跑清理 SQL、不重新启用旧本地模型。
- [ ] 8.5 汇总各批版本、验收证据、接口约定、运行和排障说明，记录持续视频/问答流/训练及框架升级等后续事项；检查所有当前必做任务真实完成后同步规格并归档，仍缺正式联调或验收时保持变更未完成。
