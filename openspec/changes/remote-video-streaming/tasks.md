## 1. 契约与共同起点

- [x] 1.1 由 02 冻结 `video-stream-analysis.v1` 的五个端点、有界 DTO、OpenAPI 与样例，明确浏览器只提交 `streamSourceId`，并以契约测试验证原始 RTSP、GPU URL 和凭据字段均被拒绝。
- [x] 1.2 由 00 核对文件归属、供应商真实能力假设和伴随变更依赖，记录新的共同起点 SHA；在 source 映射、TLS/CA、鉴权和停止能力未确认前，所有实时来源保持 disabled。

## 2. Provider 与安全边界（03）

- [x] 2.1 实现来源查询、会话启动、状态查询、事件查询和停止所需的严格 provider 类型与转换；通过单元测试覆盖未知字段、缺失字段和非法状态。
- [x] 2.2 实现批准地址、TLS/CA、鉴权和日志脱敏边界；以自动化检查证明响应和日志不包含 RTSP 地址、GPU URL 或凭据。
- [x] 2.3 覆盖协议故障语义：启动 POST 响应丢失进入 UNKNOWN 且不透明重试，事件查询允许有效空页，供应商未知状态不能被标为成功。

## 3. 持久化与生命周期（04a）

- [x] 3.1 通过 V002 增量迁移新增 `ai_stream_source`、`ai_stream_session`、`ai_stream_event` 及必要约束；在含既有图片历史的数据库上连续执行两次迁移并记录表、行摘要。
- [x] 3.2 实现来源授权、会话幂等和派发前持久化；测试匿名、无权限、跨用户来源/会话访问及重复启动冲突。
- [x] 3.3 实现事件游标、去重、排序和授权截图回存；测试重复事件、乱序事件、有效空事件页及截图归属。
- [x] 3.4 实现重启恢复、停止确认和终态保护；验证不可查询的已派发会话保持 UNKNOWN，迟到事件不能把 STOPPED、FAILED 或 UNKNOWN 覆盖为运行成功。

## 4. 前端实时事件体验（04b）

- [x] 4.1 增加已授权来源选择、会话状态、事件时间线与截图页面；用组件及浏览器测试验证空结果、错误、运行中和终态显示。
- [x] 4.2 为轮询实现离开清理、代次隔离、游标推进和终态保护；验证关闭页面不会自动停止远程会话，旧请求不能覆盖新会话。
- [x] 4.3 验证无权限、disabled 来源和直接访问均被阻止，且浏览器请求、页面状态和日志不暴露 RTSP 地址、GPU URL 或凭据。

## 5. 独立 HTTP stub 流闭环（05）

- [x] 5.1 在 provider 契约中定义已登记的合成 stub 来源、确定性事件/截图和启动/查询/停止故障场景；验证所有样例标识模拟且原始 RTSP、GPU URL 和凭据字段仍被拒绝。
- [x] 5.2 在独立 stub 服务中按模块实现来源、会话、事件、截图和停止路由；验证服务不读取业务数据库、不调用流模块内部实现，并通过契约测试覆盖有效空事件页和错误响应。
- [x] 5.3 从业务后端容器以 remote 模式调用 stub，完成来源列表、会话启动、游标事件、截图回存、页面展示和停止；以 session ID、事件 ID、截图哈希与契约版本串联模拟证据。

## 6. 本地恢复、故障与观测（06）

- [x] 6.1 用 stub 验证重复启动、启动响应丢失和服务重启对账；具备模拟查询时按外部身份恢复，无查询场景保持 UNKNOWN 且不重复派发。
- [x] 6.2 用 stub 验证事件去重、乱序、游标恢复、截图下载断流和迟到事件保护，确保 stub 下线后既有事件与截图仍可授权访问。
- [x] 6.3 用 stub 验证停止竞争、不支持停止、停止响应丢失和确认停止；只有收到对应会话确认后才进入 STOPPED，且证据不得描述为真实 GPU 停止。
- [x] 6.4 增加会话、事件、停止、UNKNOWN 和失败的 Micrometer 指标及滚动日志；验证指标可观测，日志无凭据、RTSP 秘密或未脱敏服务地址。

## 7. 功能模块与最终目录迁移

- [x] 7.1 将后端流 API、应用、领域、端口、持久化和恢复代码归入 `stream` 功能模块；运行编译、状态机、权限、事件和停止测试，确认不与 job/provider 形成循环依赖。
- [x] 7.2 将前端来源、会话、事件、截图、轮询和路由归入 `modules/ai/stream`；验证页面离开、代次隔离、游标推进、终态保护和直接访问权限。
- [x] 7.3 从最终目录冻结的同一 `08A_SHA` 创建两个同级工作树并行执行目录迁移：`codex/database-layout` 将 V002 移入 `database/migrations/stream`，`codex/remote-boundary` 将流契约、fixtures、stub 和验收证据移入 `remote-inference`；保持 V002 内容、校验值和历史兼容，回到 `codex/final-layout` 合并后修复所有活动路径。
- [x] 7.4 旧 RTSP 执行入口保持 disabled，只有未来获得真实 provider source ID 和权限证据后才启用；引用清理保留管理记录、历史事件、截图和仍使用的播放器组件。

## 8. 真实服务与发布门禁

- [ ] 8.1 从实际 RTX 5070 服务确认来源映射、方法、TLS/CA、鉴权、事件查询、停止能力和限额；资料不完整时保持任务未完成和真实流能力 disabled。
- [ ] 8.2 从实际业务后端容器完成真实 RTSP 会话、事件、截图、停止、恢复与历史读取；不以 stub、ping、端口可达或宿主机请求替代证据。
- [ ] 8.3 在 RTX 4090 48GB 正式环境复验权限、限额、事件/截图、停止和回滚；正式证据齐全前不归档本变更。
- [ ] 8.4 00 汇总 stub 与真实证据并明确区分，重跑登录、权限、图片、上传视频、实时事件、文件、历史、迁移和完整构建；只有全部真实门禁通过后才记录最终发布结论。
