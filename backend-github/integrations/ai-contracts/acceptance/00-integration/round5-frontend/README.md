# 第五轮 04b 视频与实时流前端集成验收

00 从共同起点 `f242a027a2e2827f5445bea80e517c472ff1e3c9` 以纯快进方式核实并合入
04b 最终交付 `d81a89648eba104995c4e26b6c607a850077226e`。本验收只放行基于冻结
`1.1.0` 契约的前端和可重复模拟闭环，不把模拟能力记为真实 RTX 5070 联调。

## 结论

- 38 个交付路径均属于 04b；冻结的 OpenAPI、样例、后端业务代码、前端依赖锁和
  `backend-master` 均未改变。
- 00 独立重跑 27 个 Node 检查、针对性 ESLint、04b 范围检查和两个 OpenSpec strict，全部通过。
- 00 独立生产构建成功，关键制品 SHA-256 与 04b 交付证据一致；只保留仓库既有的 CSS 顺序、
  Browserslist 和包体积告警。
- 独立浏览器回归从已授权本地来源启动实时会话，展示带偏移事件并经鉴权接口加载截图；明确点击
  停止后才发送一次停止请求，收到确认后显示 `STOPPED`。强制只读权限后直接访问实时页进入 404。
- 轮询请求从无游标开始，随后稳定携带同一游标；页面未发送原始 RTSP、GPU URL 或凭据。

## 放行与保留门禁

本次接受平台任务 4.9、4.10 及伴随流任务 4.1—4.3，形成 03→04a→04b 串行验收闭环，
可在本验收提交产生后作为 05 的新共同起点。供应商 v0.2 仍为 `UNCONFIRMED`，生产视频和实时流
能力继续硬关闭；真实图片/视频/流方法、TLS/CA、鉴权、source ID 映射、事件查询和停止能力均由
05 实际确认。05、06、6.5、07 和发布候选均未完成。

机器证据见同目录 `acceptance.json`、`build.json`、`browser.json` 和 `review.json`。
