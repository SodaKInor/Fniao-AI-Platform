# 并行开发准备记录（历史）

> 本文冻结 2026-09-03 的旧 WGAI 与 WGAI-parallel 施工环境，不是当前运行说明。

日期：2026-09-03。

## 已完成

- 在原 WGAI 建立 Git 仓库，源码基线提交为 8c7fa382a344e82eb13828d53b9fd9e018a5a461。
- origin 指向 https://github.com/SodaKInor/Fniao-AI-Platform.git；远端在准备时已存在，为公开空仓库，不重复创建或改变可见性。
- 提交作者使用经 GitHub API 确认的 SodaKInor，邮箱使用该账号 ID 对应的 GitHub noreply 地址，未伪造私人邮箱。
- 准备 10 个独立 Git worktree / 分支，各自位于 WGAI-parallel/<工作包>/code/；候选端口已登记，未启动应用、数据库或模型服务。
- 各包具备 START_HERE、AGENTS、TASKS、HANDOFF，另有 6 轮共 15 段新对话提示词。
- 41 项实施任务均有唯一主负责人；3.4 的前端部分由 04b 配合，4.7 由 00 集成执行。
- 新增分层、文件规模、公共类型、文件归属和并行隔离约束。
- 定向检查待提交文件中的令牌/私钥模式及排除路径；原数据、配置和制品按 REPOSITORY_BASELINE.md 保留本机。两处注释中的旧 JWT 示例已脱敏，不改变执行逻辑。
- OpenSpec 严格校验通过，任务映射、文档链接和交接文件完整性检查通过。

## 仍未完成

只有总任务 1.3 已勾选。01-foundation 仍需核实来源/可重建性、备份与恢复说明、入口清单及实际运行资源隔离；独立代码工作树不等于独立运行环境已经启动。公共业务契约、应用代码、前后端集成、5070 真实联调与 4090 正式验收均未完成。

## 工具检查边界

Graphify 已按要求完成 AST 更新；新 Git 忽略规则使只读参考/配置等排除文件退出当前图谱，当前图为 27213 个节点。工具报告 6 个已有 Vue 文件存在部分解析问题，不能用这些文件在图中缺少的节点推断源码不存在；没有把这些解析提示当作本次业务代码编译失败。没有执行整套应用构建、登录、推理或数据库恢复测试。

Serena 当前项目是原 WGAI，但 Vue/TypeScript 语言服务因 Node 路径未初始化；新 worktree 不通过共享实例写入不匹配的目录。相关限制写入各包约束。

原 .codex/hooks.json 包含本机 Serena 会话清理钩子。该文件保留在原目录，并从共享分支的当前版本排除，避免并行对话结束时触发本机共享服务的清理；各 worktree 按任务约束显式处理 Graphify，不继承该本机钩子。

推送与工作树最终提交状态以 Git 记录和 WGAI-parallel/WORKSPACES.json 为准；本文不是自动生成的运行健康证明。
